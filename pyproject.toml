[project]
name = "quality-of-blocking"
version = "0.2.0"
description = "Quality of Blocking (QoB) — rescoping in progress after neteng discovery (June 2026)."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
